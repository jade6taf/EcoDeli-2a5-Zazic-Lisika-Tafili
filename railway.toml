[build]
builder = "dockerfile"

[deploy]
# Désactiver complètement les health checks pour permettre le démarrage
# healthcheckPath = "/actuator/health"
# healthcheckTimeout = 300
restartPolicyType = "on_failure"
startCommand = "java -jar app.jar"

# Railway injectera automatiquement :
# - DATABASE_URL depuis le service PostgreSQL
# - PORT pour le serveur web

# Variables à ajouter manuellement via l'interface Railway :
# JWT_SECRET=votre_jwt_secret_securise
# MAIL_USERNAME=votre_email@gmail.com  
# MAIL_PASSWORD=votre_mot_de_passe_application
# STRIPE_PUBLISHABLE_KEY=pk_test_...
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
